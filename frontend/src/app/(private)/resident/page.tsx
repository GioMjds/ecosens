import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Resident",
    description: "This is the resident page",
}

export default function ResidentPage() {
    return <h1>Resident Page</h1>;
}